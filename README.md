### `README.md`

```markdown
# Sensive Blog

Блог на Django, оформленный в стиле Sensive. Проект включает посты, комментарии, динамические страницы, карту и красивую адаптивную вёрстку.

---

## ⚙️ Установка и запуск

1. Клонируй репозиторий:

```bash
git clone <URL-репозитория>
cd <имя-папки>
```

2. Установи зависимости:

```bash
pip install -r requirements.txt
```

3. Создай `.env` файл (если требуется) и настрой базу данных.

4. Запусти миграции:

```bash
python manage.py migrate
```

5. Запусти сервер:

```bash
python manage.py runserver
```

Перейди в браузере по адресу: [http://127.0.0.1:8000](http://127.0.0.1:8000)

---

## 🔧 Структура страниц

- **Главная** — `/`  
  Показывает популярные и свежие посты.

- **Детали поста** — `/post/<slug>/`  
  Отображает один пост, его текст, лайки, и комментарии.

- **Контакты** — `/contact/`  
  Показывает карту с координатами компании, форму обратной связи.

---

## ✨ Особенности проекта

### Посты
- Отображаются на главной и открываются по ссылке.
- Используется `slug` для генерации URL.
- Кнопка “Read More” ведёт на `post_detail`.

### Карта
- На странице `/contact` отображается интерактивная карта (`folium.Map`).
- Координаты задаются в `settings.py` → `COMPANY_COORDINATES`.
- В шаблоне карта подключается через фильтр `|safe`.

### Навигация
- Кнопки **Home** и **Contact** работают через `{% url '...' %}`.
- Установлен единый серый цвет (`#3a414e`) для всех пунктов меню.

### Динамические заголовки
- `<title>` каждой страницы изменяется:
  - Главная: `Sensive Blog - Home`
  - Контакты: `Sensive Blog - Contact`
  - Пост: `{{ post.title }} - Blog Details`

Настраивается через `{% block title %}` в `base.html`.

---

## 🖼 Пример данных

Чтобы увидеть посты на главной, заполни базу данных вручную или через `admin`.

---

## 📂 Статическая структура

```
your_app/
├── static/
│   ├── styles.min.css
│   ├── scripts.js
│   └── img/
│       ├── favicon.png
│       ├── logo.png
│       └── blog/
│           └── blog1.png
```

---

## 🧩 Полезные ссылки

- [Django Documentation](https://docs.djangoproject.com/)
- [Folium Map Docs](https://python-visualization.github.io/folium/)
- [HTML Title Tag (MDN)](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/title)

---

## ✅ Что сделано

- [x] Ссылки Home и Contact работают через `{% url %}`
- [x] Цвет меню одинаковый (серый `#3a414e`)
- [x] Каждая страница имеет свой `<title>`
- [x] Карта на странице `contact` встроена и работает
- [x] Посты кликабельны — ведут на `post_detail`
- [x] Используется шаблон `base.html` (опционально)

---